// Supabase is no longer used - we're using MySQL backend instead
// This file is kept for backward compatibility but not actively used
// All database operations now go through the Express API (see src/utils/api.ts)

export const supabase = null;
